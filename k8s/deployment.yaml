apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-python-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python-web
    spec:
      containers:
      - name: app-container
        image: test-api
        ports: 8080
